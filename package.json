{
  "name": "tarot-api",
  "version": "1.0.0",
  "description": "An api that records tarot games",
  "main": "index.js",
  "scripts": {
    "start": "nodemon -r dotenv/config index.js dotenv_config_path=.env.local",
    "dev": "nodemon -r dotenv/config index.js dotenv_config_path=.env.local",
    "test": "jest --watchAll --verbose",
    "fixtures": "node -r dotenv/config src/dataFixture/playerMock.js dotenv_config_path=.env.local"
  },
  "author": "Armand Deshais",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.1",
    "mongoose": "^7.4.3",
    "nodemon": "^3.0.1",
    "supertest": "^6.3.3"
  },
  "devDependencies": {
    "jest": "^29.6.3"
  }
}
